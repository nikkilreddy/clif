================================================================================
CLIF — HACKATHON WINNING DEMO SCRIPT & JUDGES DEFENSE WALKTHROUGH
================================================================================
Project: CLIF (Cognitive Log Investigation Platform)
GitHub Repository: https://github.com/nikkilreddy/clif
Author / Speaker: Ram Nikhil Reddy (@nikkilreddy)
Duration: ~3 Minutes (180 Seconds)
Tone: Conversational, Confident, Punchy Technical Authority

================================================================================
🎯 THE KNOCKOUT QUESTION: "WHAT DID YOU BUILD THAT THE API DIDN'T GIVE YOU?"
================================================================================
No API exists that can do this. If you pass 10,000 logs/sec to OpenAI or Claude, 
you hit rate limits, pay thousands in API bills, and get hallucinated CVEs.
What WE built from scratch with ZERO API wrappers:
1. Rust Vector VRL data canonicalization engine.
2. Custom Go streaming consumer with LZ4 compression & UUID5 deduplication at 35k EPS.
3. 60-feature extractor across 7 layers & trained dual LightGBM + Autoencoder ONNX models.
4. 5-engine Hunter correlation & topological Attack Graph generator.
5. Calibrated Verifier forensic decision matrix with Graceful Degradation (Inconclusive state).

================================================================================
WORD-FOR-WORD CONVERSATIONAL SCRIPT
================================================================================

--------------------------------------------------------------------------------
📍 SCENE 1: THE TARGET PERSONA & THE PAIN (00:00 – 00:30)
--------------------------------------------------------------------------------
[ON SCREEN]: 
- Open Dashboard Home at http://localhost:3001
- Show the dark-mode SOC dashboard with active agent telemetry cards and live counters.

[SPEAK]:
"Hey everyone! 

If you talk to any Tier-1 or Tier-2 SOC Incident Responder today, they are drowning. They face over 10,000 alerts every single shift, and more than 45% are noisy false positives. When a real attack happens, manually digging through fragmented logs takes hours.

Traditional SIEMs rely on brittle IF-ELSE playbooks that break the second an attacker tweaks a payload. And simply throwing logs into a generic ChatGPT prompt is too slow, expensive, and hallucinates fake CVEs.

So, we built CLIF—the Cognitive Log Investigation Platform. 

CLIF replaces manual playbooks with an autonomous 3-agent pipeline—Triage, Hunter, and Verifier—that turns millions of raw logs into verified forensic verdicts in under sixty seconds."


--------------------------------------------------------------------------------
📍 SCENE 2: ATTACKING SECUREBANK & HIGH-SPEED DATA PIPELINE (00:30 – 01:05)
--------------------------------------------------------------------------------
[ON SCREEN]: 
- Show SecureBank at http://localhost:5001 (our target banking application).
- Show the Live Feed at http://localhost:3001/live-feed.
- Open a split terminal on the right and run:
  python3 demo/securebank/attack.py --fast

[SPEAK]:
"Let’s see it live. We’re putting on our attacker hat and targeting SecureBank, our vulnerable banking application.

In the terminal on the right, I'm firing off a multi-stage attack: port scanning, brute-forcing admin credentials, and executing SQL injection.

Over on our Live Feed, SecureBank is pumping out security logs. 

Now, here is the answer to the biggest question: What did we build that the API didn't give us? 

No API in the world can ingest high-velocity enterprise streams. We built custom Rust Vector VRL transforms to normalize raw logs, buffered them in a Redpanda stream, and engineered a high-performance Go consumer using zero-allocation buffers, LZ4 compression, and deterministic UUID5 deduplication to batch-insert into ClickHouse at over 35,000 events per second with zero packet loss."


--------------------------------------------------------------------------------
📍 SCENE 3: TWO-MODEL ENSEMBLE & HUNTER ATTACK GRAPH (01:10 – 01:50)
--------------------------------------------------------------------------------
[ON SCREEN]: 
- Navigate to Investigations at http://localhost:3001/investigations
- Click the escalated investigation and zoom into the interactive Attack Graph.

[SPEAK]:
"Now, look at the AI architecture. We don't use a single API prompt wrapper—we engineered a cooperative two-model ensemble.

First, our Triage Agent extracts 60 numerical features across seven domain layers in just ten microseconds. It runs two models in parallel:
- Model 1 is a supervised LightGBM ONNX classifier for known attack signatures.
- Model 2 is an unsupervised Deep Neural Autoencoder for zero-day behavioral anomalies.

Here’s the beauty: What if one model fails? If an attacker uses a stealthy zero-day that LightGBM scores as zero, the Autoencoder catches the abnormal reconstruction error at 0.92 and escalates it anyway.

That triggers our Hunter Agent. We built five correlation engines from scratch—including Sigma rules, temporal causality, and 3-sigma statistical anomalies—to automatically construct this Attack Graph. It visually connects the attacker's external IP, shows how they compromised the admin account, and pinpointed the exact database table they exfiltrated."


--------------------------------------------------------------------------------
📍 SCENE 4: VERIFIER FORENSIC VERDICT & HANDLING BEING WRONG (01:50 – 02:25)
--------------------------------------------------------------------------------
[ON SCREEN]: 
- Scroll down to the Verifier section.
- Point out the Verdict Badge (True Positive), Confidence (94%), and the plain-English timeline.

[SPEAK]:
"Next, our Verifier Agent steps in as the Senior Forensic Investigator. It evaluates evidence coherence across the graph and gives us a calibrated verdict: True Positive, Priority 1 Critical, with 94% confidence.

Now, what happens when the model is wrong or data is missing? 

We built strict Graceful Degradation: if logs are conflicting or incomplete, the system will NEVER guess or trigger an unsafe automated action. It safely flags the incident as 'Inconclusive' with wider confidence bounds.

Plus, it automatically generates this plain-English forensic narrative and step-by-step timeline, ready for human analysts or courtroom submission."


--------------------------------------------------------------------------------
📍 SCENE 5: AUTOMATED BENCHMARK, SCALE & CLOSING (02:25 – 03:00)
--------------------------------------------------------------------------------
[ON SCREEN]: 
- Switch full-screen to terminal and run:
  python3 eval_harness.py
- Show all 20 test cases passing with green checkmarks.

[SPEAK]:
"To prove this is real engineering and not slideware, we built an automated evaluation harness testing 20 distinct cyberattack and failure scenarios. 

In our terminal, you can see all 20 tests passing with 100% precision, zero alert fatigue, and full tool-failure recovery.

If you ask what breaks at a million events per second? ClickHouse memory pressure on unpartitioned queries and Kafka partition lag—which we mitigate through horizontal sharding and scaling our Go worker pools.

If you remove the AI, this system collapses into a passive, dumb log bucket. CLIF turns hours of stressful log digging into sub-minute forensic clarity. 

Thank you!"

================================================================================
QUICK REFERENCE: THE 5 JUDGES QUESTIONS CHEATSHEET
================================================================================
1. Who has the problem?
   -> Tier-1 and Tier-2 SOC Incident Responders drowning in 10k daily alerts (45% FP rate).
2. What is the non-obvious hard part?
   -> 60-feature leakage auditing (7 rounds) & zero-loss Go streaming at 35,000+ EPS.
3. What did you build that the API didn't give you?
   -> Rust Vector VRL transforms, Go LZ4 consumer (35k EPS), dual LightGBM/Autoencoder ONNX ensemble, 5-engine Hunter Attack Graph, and Next.js visualizer. Zero prompt wrappers.
4. Why does it break without AI?
   -> Without AI, logs cannot be scored, multi-host attack graphs cannot be built, and alerts cannot be investigated.
5. What breaks at scale (1M EPS)?
   -> ClickHouse memory pressure and Kafka partition contention (mitigated by sharding & worker scaling).
